package ${packageName};


public class ${underscoreToCamelCase(classToResource(fragmentClass))}Model implements ${underscoreToCamelCase(classToResource(fragmentClass))}Contract.Model {
    protected String TAG = getClass().getSimpleName();
}
